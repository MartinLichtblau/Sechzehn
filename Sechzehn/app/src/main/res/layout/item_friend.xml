<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/dim_large"
    android:gravity="center_vertical|start"
    android:orientation="vertical"
    android:paddingEnd="@dimen/dim_normal"
    android:paddingLeft="@dimen/dim_normal"
    android:paddingRight="@dimen/dim_normal"
    android:paddingStart="@dimen/dim_normal">

    <TextView
        android:id="@+id/material_drawer_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:gravity="center_vertical|start"
        android:lines="1"
        android:maxLines="1"
        android:textDirection="anyRtl"
        tools:text="Some drawer text" />

    <TextView
        android:id="@+id/material_drawer_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif"
        android:gravity="center_vertical|start"
        android:lines="1"
        android:maxLines="1"
        android:textDirection="anyRtl"
        tools:text="Some drawer text" />
</LinearLayout>